apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rec-engine.fullname" . }}-config
  labels:
    {{- include "rec-engine.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
