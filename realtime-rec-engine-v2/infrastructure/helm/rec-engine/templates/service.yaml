apiVersion: v1
kind: Service
metadata:
  name: {{ include "rec-engine.fullname" . }}
  labels:
    {{- include "rec-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rec-engine.selectorLabels" . | nindent 4 }}
