[pytest]
testpaths = tests
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require running services)
    slow: Slow tests
asyncio_mode = auto
addopts = -v --tb=short
